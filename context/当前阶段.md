# 当前阶段：Reference Freeze Candidate

> 权威架构文档：`docs/ARCHITECTURE.md`
> 权威路线图：`docs/ROADMAP.md`

Reference Runtime 已完成全部 6 层（Rule/Pattern/Evidence/Knowledge/Consensus/Conformance），
全量 381 个测试通过（304 Reference + 77 Production），135 项 Conformance 检查 100% 通过。

当前处于 Reference Freeze Candidate 阶段，等待最终冻结。

标准环境已就绪: uv + Python 3.11（`.python-version`），依赖已补全，`uv sync --all-extras` 一键搭建。

Production Runtime (src/) 部分实现，尚未与 Reference Runtime 对齐。
未来对齐必须通过 RuntimeAdapter 或其他经批准的集成机制，不得大规模重构。
