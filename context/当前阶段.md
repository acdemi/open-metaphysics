# 当前阶段：Knowledge 语料建设（Phase 7.x）+ 三大计算域 Integration Ready

> 权威架构文档：`docs/ARCHITECTURE.md`
> 权威路线图：`docs/ROADMAP.md`
> 能力状态：`docs/governance/CAPABILITY_STATUS.md`
> （快速参考；详细状态以 `docs/PROJECT_STATUS.md` 权威）

**当前主线**（2026-08-17）: **Knowledge 语料建设**

- 三大计算域均 **Integration Ready**: Qimen / BaZi / Ziwei（各契约 v1.0.0 Frozen +
  Reference Certified + 24 向量 + 集成就绪）
- Knowledge 层（只读引用层）: Architecture **FROZEN**（KB-001~020）/ Pipeline
  **VALIDATED** / Corpus **PARTIAL**（Ziwei 63 节点 / 37 关系 / 10 引用, Phase 7.1.6）
- **最新 Sprint Phase 7.2A**: Schema Admission Gate —— shen_sha / auxiliary_star 均
  裁定 **A（已存在, 无需 ACP）**, 零数据生产, 待人工 Evidence Review + 7.2B 生产授权

（历史 Qimen/Ziwei 完成情况保留如下）

Qimen Phase 5 系列完成 → **Qimen Domain Frozen and Certified**：
- 排盘核心（转盘法, engine 0.3.0）→ 规则裁定（12 Freeze）→ 24 规范向量
- 契约冻结 v1.0.0（`docs/specification/QIMEN_BEHAVIOR_CONTRACT.md`）
- 适配层 / ABI / 回归防护网齐备
- **Reference 认证**: reference/qimen 自包含实现（无 src 导入）+ 独立测试套件
  + 14/14 QC 审计 + 30/30 确定性等价 + 认证工件（`docs/qimen/reference_certification.md`）
- 双实现验证: Product Runtime 与 Reference Runtime 逐字节一致

标准环境：uv + Python 3.11（`.python-version`），`uv sync --all-extras` 一键搭建

---

## 领域能力生命周期（Domain Capability Lifecycle）

    Domain Calculation Layer     ← 确定性排盘/推演算法
                ↓
    Behavior Contract Layer      ← 冻结契约 + Golden Vectors
                ↓
    Reference Verification Layer ← Reference 认证 + 双实现验证

**官方架构概念 + 标准流程**（`docs/ARCHITECTURE.md` §1 + `docs/governance/CAPABILITY_LIFECYCLE.md`）：
5 阶段（Exploration → Calculation Runtime → Behavior Contract → Reference
Certification → Certified Capability）/ 6 状态（Experimental → Implemented →
Contract Candidate → Contract Frozen → Reference Certified → Integration Ready）。
Qimen = Integration Ready（完整生命周期）；BaZi/Ziwei = **Integration Ready**；Liuyào = Implemented
（只标实际状态，不虚报，见 `docs/governance/CAPABILITY_STATUS.md`）。

Qimen 域只产生**确定性观测结果**，不包含：
Interpretation / Recommendation / Narrative Generation / Belief Scoring /
LLM Reasoning / RAG Knowledge / Consensus Decision（属未来层，不得混入冻结计算能力）。

---

## Qimen 冻结与变更政策（重要）

1. **Qimen 算法不得随意修改**（`src/openmetaphysics/domain/qimen/` 与
   `reference/qimen/` 算法文件）。
2. 任何规则变更必须完整执行：
   - **ACP** 流程（Architecture Change Proposal, 等待人工批准）
   - **契约版本递增**（Behavior Contract, 当前 v1.0.0）
   - **Golden Vector 迁移**（24 向量, 不可原地修改）
   - **Reference Runtime 更新**（双实现同步 + 重新认证）
3. 冻结工件（Frozen Artifacts）:
   - Qimen Behavior Contract v1.0.0（`docs/specification/QIMEN_BEHAVIOR_CONTRACT.md`）
   - Golden Vector 数据集（`docs/qimen/golden_vectors.json`）
   - Reference Certification Report（`docs/qimen/reference_certification.md`）
