# 当前阶段：Phase 6 — 规则层与知识层架构设计（纯设计，禁止编码）

**阶段性质**：架构设计 + Schema 定义（Pydantic v2 模型输出到文档，不修改 src/）
**目标**：建立统一规则层（Rule Layer）和知识层（Knowledge Layer），为未来全部命理体系（八字、紫微、奇门、六爻、梅花、六壬）提供共识数据基础。

**核心原则**：
1. Rule First. Knowledge Second. LLM Last.
2. 凡是规则能表达的，禁止交给 LLM。
3. 共识从 Weighted Average 升级为 Evidence Based Consensus。

**本阶段严格禁止**：
- 修改 `src/openmetaphysics/agents/` 下任何已稳定模块（八字、紫微）。
- 实现数据库持久化（Postgres/Neo4j）。
- 编写任何运行时业务代码（只准写 Pydantic Schema 定义，且放在 `docs/` 或 `schemas/` 草案目录）。

**交付物要求**：输出 10 项设计文档，含 Mermaid ER/流程图、ADR、JSON Schema、Pydantic Models。