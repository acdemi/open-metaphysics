# 对话输出归档

> 每次对话（每轮会话）结束前，将本轮产出追加记录于此。追加式，不覆盖历史。
> 约定详见 `AGENTS.md` → Conversation Workflow Convention。

## 2026-08-12 建立对话工作约定 + 修复工具输出乱码

- **分支**：`work/convention-output-archive`
- **状态**：已合并（用户于 2026-08-12 批准合并到 main）

### 产出清单

| 文件 | 变更 | 状态 |
|------|------|------|
| `AGENTS.md` | 新增「Conversation Workflow Convention」章节：输出归档、分支工作流、用户把控合并 | 已合并 |
| `context/归档.md` | 本文件，归档体系建立（首次记录） | 已合并 |
| `context/开发规范.md` | 新增第 9 条「对话工作流约定」（指向 AGENTS.md 细则） | 已合并 |
| `context/项目状态.md` | 末尾补充开发流程约定 + UTF-8 工具链说明 | 已合并 |
| `docs/PROJECT_STATUS.md` | 更新日期至 2026-08-12，新增「11. 开发流程约定」章节 | 已合并 |

### 机器级修复（不在 git 内）

| 路径 | 变更 | 状态 |
|------|------|------|
| `C:\Users\lkl\Documents\PowerShell\Microsoft.PowerShell_profile.ps1` | 头部新增 UTF-8 编码统一块（`$OutputEncoding` / `[Console]::OutputEncoding` / `InputEncoding` = UTF8） | 已写入（用户交互终端生效） |
| `C:\Users\lkl\AppData\Local\Programs\opencode-utf8\pwsh.exe` | UTF-8 pwsh wrapper（csc 编译，basename=pwsh 使 opencode 识别为 PowerShell 并追加 `-Command`；注入 `chcp 65001` + 编码引导后转发真实 pwsh） | 已编译，字节级验证通过 |
| `C:\Users\lkl\.config\opencode\opencode.json` | 全局 `shell` 指向 wrapper | 已写入，需重启 opencode 生效 |
| 本仓库 `.git/config` | `git config core.quotepath false` | 已生效（非跟踪文件） |

### 乱码根因

- 系统活动代码页 936 (GBK)；工具以 UTF-8 解析子进程 stdout，GBK 字节被误读。
- opencode 强制 `pwsh -NoLogo -NoProfile -NonInteractive -Command`，profile 不加载 → profile 修复无效。
- 子进程管道化 spawn，`HKCU\Console\CodePage=65001` 注册表方案实测无效 → 已回滚。
- 最终方案：shell wrapper 在命令前注入 UTF-8 引导，字节层强制 UTF-8。

### 遗留事项

- 需重启 opencode 后验证工具输出乱码消失。
- wrapper 交互式终端（TUI 持久 shell）分支未实测，已按参数透传设计，风险低。
- 若重启后仍有乱码，优先检查 wrapper 路径与 `shell` 配置是否被其它配置文件覆盖。

## 2026-08-13 Phase 6.5 BaZi Reference 认证 + Phase 6.6 Integration Ready 收尾

- **分支**：Phase 6.5 → `work/bazi/phase6.5-reference-certification`；Phase 6.6 → `work/bazi/phase6.6-integration-ready`
- **状态**：均已合并（main 历史重写，见下方「历史重写」；merge commits `5abe56d` / `c297c84`，2026-08-13 用户批准）

### 产出清单

| 文件 | 变更 | 状态 |
|------|------|------|
| `docs/bazi/BAZI_BEHAVIOR_CONTRACT.md` | 新增，契约 v1.0.0 Frozen（BC-001~014） | 已合并（5abe56d） |
| `reference/bazi/`（README/astronomy/domain/tables/__init__） | Reference 独立实现（无 src 导入） | 已合并（5abe56d） |
| `reference/tests/test_bazi_equivalence.py` | 24/24 Production == Reference 等价 | 已合并（5abe56d） |
| `docs/bazi/BAZI_REFERENCE_AUDIT.md` / `BAZI_REFERENCE_CERTIFICATION.md` | 14/14 BC 审计 + 认证 | 已合并（5abe56d） |
| `docs/governance/CAPABILITY_STATUS.md` | BaZi 状态升级 Reference Certified → Integration Ready | 已合并（c297c84） |
| `docs/SCHEMAS.md` | §3.1 BaZi Schema 登记（Phase 6.6 Task A）+ 全文件损坏代码围栏修复 + 头部状态刷新 | 已合并（c297c84） |
| `docs/bazi/BAZI_INTEGRATION_READINESS.md` | 新增，集成边界审查 7/7 PASS | 已合并（c297c84） |
| `docs/INTERFACES.md` | 修复损坏代码围栏（7 处）+ 文字损坏（emperature→temperature, ender→render） | 已合并（c297c84） |

### 历史重写（2026-08-13 用户批准）

- 原 `5d3fc04`（Phase 6.5）违反约定直接提交于 main → 移入
  `work/bazi/phase6.5-reference-certification` 分支，main 重置回 `1b9cc16` 后
  经 `--no-ff` merge 合入（`5abe56d`）。
- Phase 6.6 分支（`6f423d1`）随后合入（`c297c84`）。
- 最终 main 树与重写前逐字节一致（`git diff 6f423d1 main` 为空），内容零丢失。
- `origin/main` 已 force push 同步。

### 遗留事项

1. ~~**Phase 6.5 提交违反分支约定**~~ ✅ 已通过历史重写解决（见上）。
2. 可选后续：JSON Schema 导出（`Model.model_json_schema()`）未实现，不影响 Integration Ready 状态。
3. 已合入 main 的分支（`work/bazi/phase6.5-reference-certification` / `work/bazi/phase6.6-integration-ready`）可保留或删除，由用户决定。

## 2026-08-13 Phase 6.7.1 — Ziwei 算法稳定化与规则裁定

- **分支**：`work/ziwei/phase6.7.1-algorithm-stabilization`
- **状态**：待合并（未获用户指令，禁止合并；按约定由用户把控）

### 会话内事件：并行 Phase 7.0 遗留改动

- 会话开始后工作树出现并行会话（Phase 7.0 Ziwei 评估）未提交改动：
  `docs/governance/ziwei/`（4 份未跟踪）+ `CAPABILITY_STATUS.md` /
  `context/项目状态.md` 修改。
- 经用户确认：「在其上继续，无损合并」—— Phase 7.0 产出全部保留，
  本 Sprint 文档沿用 ZW-001~017 编号，TEST_COVERAGE_REVIEW 以追加章节
  方式扩展，未覆盖任何 Phase 7.0 内容。
- 注意：Phase 7.0 文档内日期标注为 2026-08-09（实为 2026-08-13 写入），
  未改动（无损原则）。

### 产出清单

| 文件 | 变更 | 状态 |
|------|------|------|
| `docs/governance/ziwei/ZIWEI_ALGORITHM_AUDIT.md` | 新增，计算层 16 问审计 + 发现 F-1~F-11 | 待合并 |
| `docs/governance/ziwei/ZIWEI_ALGORITHM_ASSUMPTIONS.md` | 新增，ZW-A1~A15 假设表 | 待合并 |
| `docs/governance/ziwei/ZIWEI_RULE_DECISION.md` | 新增，ZW-001~017 裁定（14 Freeze Candidate / 3 Deferred）+ 歧义登记 A-1~A-8 + Evidence Ledger 观察 | 待合并 |
| `docs/governance/ziwei/ZIWEI_CROSS_DOMAIN_BOUNDARIES.md` | 新增，ZB-01~09 / ZQ-01~04 正式登记 | 待合并 |
| `docs/governance/ziwei/ZIWEI_GOLDEN_VECTOR_READINESS.md` | 新增，24 候选向量矩阵（未生成正式向量） | 待合并 |
| `docs/governance/ziwei/ZIWEI_TEST_COVERAGE_REVIEW.md` | 追加 §5（Phase 6.7.1 补测 +21 清单与矩阵），Phase 7.0 内容原样保留 | 待合并 |
| `docs/governance/ziwei/ZIWEI_CAPABILITY_ASSESSMENT.md` / `ZIWEI_RULE_INVENTORY.md` / `ZIWEI_CROSS_DOMAIN_PRECHECK.md` | Phase 7.0 会话产出（未跟踪 → 随本分支提交） | 待合并 |
| `tests/test_ziwei.py` | +21 例（12→33）：定局表快照/全 5 局/星曜位置/时区链/时辰窗/闰月/立春界/序列化 | 待合并 |
| `docs/SCHEMAS.md` | §3.2 勘误（calendar_note 位于 ZiweiChart）+ extra="forbid" + 状态注记 | 待合并 |
| `docs/governance/CAPABILITY_STATUS.md` | Phase 7.0 基础上追加 Phase 6.7.1 注记 + Ziwei 节更新（33 测试 / 工件清单 / 6.7.2 入口条件） | 待合并 |
| `context/项目状态.md` | 追加 Phase 6.7.1 段 | 待合并 |

### 验证

- 全仓库 pytest：**578/578 通过**（557 基线 + 21 新增；Qimen 24/24、BaZi 等价 30/30 含于内）。
- `ruff check` + `ruff format --check`：全部通过。
- ZERO TOUCH 核验：`src/`（含 domain/qimen）、`reference/qimen`、`docs/qimen`、
  `reference/bazi`、`docs/bazi`、`CAPABILITY_LIFECYCLE.md`、
  `DOMAIN_CAPABILITY_TEMPLATE.md`、`docs/specification/` —— diff 全部为空。

### 遗留事项（Phase 6.7.2 入口前置，均须人工裁定）

1. **A-1**：`ZIWEI_POS` 定局表 4/5 局（木三/金四/土五/火六）与主流《紫微星诀》起宫/步长系统性差异 —— 维持现状为规范 or ACP 修正（当前行为已被 150 组合测试锁定，两个方向证据均可用）。
2. **A-2**：廉贞偏移 -9 vs 主流 -8（紫微在子 → 现廉贞在卯，主流在辰）。
3. **ZW-001**：lunar_day 越界 KeyError 是否可接受（当前已锁定为行为）。
4. **sxtwl 锁版策略**：契约化前须精确 pin + 历法向量回归。
5. 解释域遗留（不在计算域契约范围）：`pattern_matcher.py` 模块遮蔽死代码（F-1）、`ZiweiExplainer` 引用不存在字段且未接线（F-2）。
