# BaZi Cross-Domain Boundaries

> **日期**: 2026-08-09（Phase 6.4 Task E）
> **目的**: 正式记录各域之间**有意的、既定的**行为差异, 防止被误报为
> "框架 bug"。当统一运行时（OpenMetaphysics）同时计算多域时, 以下差异
> 是**领域规则**, 而非实现不一致。
> **冻结状态**: BaZi 侧差异已随 B1~B6 冻结（`BAZI_FREEZE_REVIEW.md`）;
> Qimen 侧由契约 v1.0.0 冻结（不可修改）。

---

## 1. 域间差异登记（BaZi ↔ Qimen）

| # | 维度 | BaZi（本域, 已冻结） | Qimen（契约 v1.0.0） | 性质 |
|---|------|----------------------|----------------------|------|
| D-01 | 晚子时（23:00-24:00）日柱 | **23:00 换日**（B3, 向量 B_late_002 锁定） | **不换日**（D14 裁定） | 流派差异, 各域独立裁定 |
| D-02 | 真太阳时 | **不使用**（钟表时, metadata 声明） | **使用**（D13, 有坐标时） | 历法精度取舍, 各域独立 |
| D-03 | 节气边界比较 | UTC 时刻（B1/B2, 与 Qimen D1 机制相同） | UTC 判定（D1） | **一致机制**（无差异） |
| D-04 | 性别回退 | UNKNOWN 按男处理 + `gender_assumed`（B6） | Qimen 输入无性别依赖 | 域输入差异 |
| D-05 | 计算输出 | 四柱/十神/藏干/纳音/大运 | 九宫盘（星/门/神/地盘/天盘/空亡） | 域对象差异（非行为冲突） |

## 2. 框架级边界（通用, 非 BaZi 专属）

| 项 | 声明 |
|----|------|
| 确定性 | 所有域: 相同输入 ⇒ 字节级相同输出（QC-001 通用语义） |
| 领域边界 | 所有域只产生**确定性观测**; 解释/建议/叙述/评分/LLM/RAG/Consensus 在域外（ARCHITECTURE.md §1） |
| 契约优先 | 域间若有表面冲突, 以**各自冻结契约**为裁判, 不互相覆盖 |
| 变更通道 | 任何域行为变更 = ACP + 契约版本递增 + 向量迁移（CAPABILITY_LIFECYCLE.md §5） |

## 3. 使用规则

1. 跨域对照测试（如未来 Consensus/联合排盘）**不得**断言域间输出一致;
   只断言各自契约规定的行为。
2. 差异被误报为 bug 时, 引用本登记表 D-01/D-02 作为判定依据。
3. 本登记表**不可修改**已冻结行为; 任何改动须 ACP。
