# Packages

Shared packages across the monorepo.

## Structure (planned)

```
packages/
├── proto-go/       # Auto-generated Go protobuf code
├── ts-client/      # TypeScript API client (OpenAPI codegen)
└── shared-types/   # Cross-language type definitions
```
